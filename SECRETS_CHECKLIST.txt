# Online secrets checklist (GitHub Actions + Streamlit Cloud)
#
# DO NOT commit real values. Copy names into:
#   GitHub → Settings → Secrets and variables → Actions
#   Streamlit Cloud → App settings → Secrets (TOML)
#
# Required for Gmail alerts:
#   GMAIL_ADDRESS
#   GMAIL_APP_PASSWORD
#   GMAIL_TO
#
# Optional:
#   TELEGRAM_BOT_TOKEN
#   TELEGRAM_CHAT_ID
#   DISCORD_WEBHOOK_URL
#   ALPHA_VANTAGE_API_KEY
#
# Scan defaults live in .github/workflows/scan.yml
# (WATCHLIST_SOURCE=sp500, GMAIL_MIN_SCORE=80, …)
#
# Full steps: see DEPLOY.md
